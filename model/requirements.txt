# PyTorch stack for the trees-outside-forests U-Net (model/).
# Install with model/tools/setup_env.sh, which points pip at the wheel index
# for the chosen flavour (cpu, cu<ver> or rocm<ver>); the packages are the same.
torch>=2.4
torchvision>=0.19
segmentation-models-pytorch>=0.4
rasterio>=1.3
numpy>=1.26,<3
pandas>=2.2
pyyaml>=6
psutil>=5.9
matplotlib>=3.8
tqdm>=4.66
